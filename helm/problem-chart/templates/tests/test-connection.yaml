apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "problem-chart.fullname" . }}-test-connection"
  labels:
    {{- include "problem-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "problem-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
